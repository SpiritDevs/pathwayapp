# Task F0 — Foundation: issue-domain contracts + seams

Repo: /Users/coreybaines/GitHub/pathwayapp-issues-build (Bun monorepo, Effect 4, TypeScript). Work ONLY here.

READ FIRST (in this order):

1. docs/issues-build/interface-freeze.md — the BINDING spec. Implement its sections 1, 2, 3 and the two seam files from section 5/8 (F0 row of the ownership map). Frozen names are frozen.
2. /private/tmp/claude-501/-Users-coreybaines-GitHub-pathwayapp/21f4374e-b3a6-4301-813d-f1f7860f47f0/scratchpad/scout-server.md — server/contracts conventions.
3. packages/contracts/src/orchestration.ts and packages/contracts/src/settings.ts and packages/contracts/src/rpc.ts — match their exact style (branded IDs, Schema.Struct conventions, optionalKey patterns, method registry style, RpcGroup registration).

DELIVERABLES (exclusive file list — touch nothing else):

1. packages/contracts/src/issues.ts (new) — every schema/type/enum/command/result/error from freeze §1–2, exported. Use Effect Schema exactly as orchestration.ts does (Schema.Literal unions, Schema.NullOr, branded string IDs via the same brand pattern used for ThreadId/ProjectId, TaggedError style used by DomainError for IssuesDomainError).
2. packages/contracts/src/rpc.ts — add the four WS_METHODS entries + four Rpc declarations + register in WsRpcGroup, per freeze §3. Follow existing declarations verbatim in style (payload/success/error unions, stream: true for the two subscriptions).
3. packages/contracts/src/settings.ts — add issueDelegation + agentActors + AgentActorRuntimeConfig to ServerSettings AND ServerSettingsPatch per freeze §3, with the same withDefaults/optionalKey machinery the file already uses.
4. packages/contracts/src/index.ts (or however the package exports modules — inspect it) — export the new issues module the same way orchestration is exported.
5. packages/shared/src/fractionalIndex.ts (new) — orderKeyBetween(a: string | null, b: string | null): string, base-62 midpoint fractional indexing: generates a key strictly between a and b (null a = before all, null b = after all); plus orderKeyInitial(): string and a rebalance helper orderKeysForCount(n): string[]. Deterministic, no Date/random. Export from the shared package index like its siblings. Include a small pure test file packages/shared/src/fractionalIndex.test.ts covering between/ends/adjacent-key cases in the same test style as neighboring \*.test.ts files (bun/vitest — copy the local idiom). DO NOT RUN the tests.
6. apps/server/src/issues/IssuesGateway.ts (new) — the Context.Service tag + interface EXACTLY as freeze §5 defines (import types from @pathwayos/contracts). Interface only, no implementation, no layer.
7. apps/server/src/issues/delegation/IssueDelegationService.ts (new) — Context.Service tag with { start: () => Effect<void, never, Scope.Scope>; state: Effect<DelegationQueueState, IssuesDomainError> }, matching the CloudSyncWorker service-declaration style.

RULES:

- Never use `any`. No runtime logic in packages/contracts (schema-only package).
- Do NOT run vp check / typecheck / lint / tests / dev servers / build. Validation is deferred by design. You may rely on careful reading of neighboring code for correctness.
- Do not commit. Leave changes in the working tree.
- When done, print a summary: files created/modified with line counts, any deviations from the freeze doc and why (deviations should be rare and justified only by existing-code constraints).
