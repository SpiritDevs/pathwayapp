# Task F1 — Convex issues domain

Work in the current repo root (a pathwayOS worktree). Bun monorepo.

READ FIRST:
1. docs/issues-build/interface-freeze.md — BINDING spec. You implement §4 (Convex) exactly, including §10 amendments (issue.setDelegationStatus command; comment/reaction/event commands bump parent issue syncSeq WITHOUT touching updatedAt).
2. /private/tmp/claude-501/-Users-coreybaines-GitHub-pathwayapp/21f4374e-b3a6-4301-813d-f1f7860f47f0/scratchpad/scout-convex.md — conventions, auth envelope, exemplars.
3. packages/contracts/src/issues.ts — the contract types your fields must match 1:1 (field names/nullability). Convex uses its own `v` validators; keep exact parity.
4. Exemplars: infra/connect-convex/convex/schema.ts, cloudSync.ts, email.ts, http.ts, environmentAuthorization.ts (requireEnvironmentPrincipal), authorization.ts, src/tenancy.ts.

DELIVERABLES (exclusive ownership — touch ONLY these):
- infra/connect-convex/convex/schema.ts — append the issue-domain tables per freeze §4 (all with tenantId/ownerUserId/syncSeq/createdAt/updatedAt/deletedAt + by_tenant_sync index + natural lookup indexes listed there).
- infra/connect-convex/convex/issues.ts (+ issuesCommands.ts / issuesQueries.ts if you split; keep modules cohesive) — `issues:executeCommand` (internalMutation), `issues:mirrorDelta` (internalQuery), `issues:issueDetail` (internalQuery) per freeze §4. Use queryGeneric/mutationGeneric-style factories like the rest of the codebase (internalMutationGeneric / internalQueryGeneric). Pure helpers (seeding defaults, identifier formatting, guardrail checks, command application) should live in infra/connect-convex/src/issuesLogic.ts (also yours) so convex modules stay thin — mirror how tenancy.ts is used.
- infra/connect-convex/convex/http.ts — append the three routes (POST /v1/issues/command, /v1/issues/mirror, /v1/issues/detail) using the exact same bearer→sha256→runMutation/runQuery envelope as /v1/sync/batches.

KEY SEMANTICS (from freeze §4 + §10 — implement all):
- Every write allocates syncSeq values from issueMeta.nextSyncSeq (per tenant) and stamps touched rows.
- First command for a tenant seeds: issueMeta (workspaceKey "WS"), workspace default states, and the human actor row for the principal's ownerUserId. team.create seeds team default states.
- issue.create: allocate number from issueCounters (scopeKey "team:<id>" or "workspace"), identifier = `${key}-${number}`; agent attribution forces triage state + triaged:false; default state else first backlog-category state; orderKey: append after current max (import orderKeyBetween from @pathwayos/shared if resolvable from convex code — if the convex tsconfig cannot import it, inline a minimal midpoint helper in src/issuesLogic.ts and note it).
- issue.moveTeam: new number from destination counter, old identifier → issueAliases row, state remapped to same-category state in destination (fallback: destination default), event recorded.
- issue.purge: hard-delete the doc + comments/reactions/relations/links; forbidden for agent attribution; purge emits a final syncSeq bump recorded in a tombstone issueEvents row (kind "issue.purged") so the mirror can emit remove.
- state.delete: migrate issues to migrateToStateId (must be same category, else error invalid).
- Soft deletes everywhere else: set deletedAt, bump syncSeq.
- Every command appends an issueEvents row (kind = command type, sanitized payload, actorId, threadRef for agent attribution) and bumps the parent issue syncSeq where applicable.
- cycle materialization: on any command touching a team whose cycleConfig.enabled, lazily ensure current + next cycle rows exist (compute from startDayOfWeek/durationWeeks/cooldownWeeks); autoRollover: when a cycle has ended, move its incomplete issues to the next cycle. Keep this logic in issuesLogic.ts with pure date math (caller passes now from Date.now() — allowed in Convex).
- mirrorDelta: rows where syncSeq > sinceSeq ordered ascending, limit (default 500), one row per doc as { table, doc } with table tags exactly matching the contracts IssuesEntityRow tags; include workspaceKey + viewerUserId (principal ownerUserId) + nextSeq + hasMore. Exclude descriptionMd from issue docs. Purged docs appear as { table: "issues", doc: { id, purged: true, syncSeq } } — document this shape in a comment; the mirror translates it to a remove.
- issueDetail: full IssueDetail shape (descriptionMd, comments incl. soft-deleted flagged, reactions, events ordered by createdAt desc limit 200).

RULES:
- Never use `any`. Match schema.ts / http.ts style exactly.
- Do NOT run codegen, vp check, typecheck, lint, tests, deploys, or dev servers. Validation is deferred.
- Commit when done: single commit, title `[new feature] Add Convex issues domain (schema, commands, mirror, http routes)`, body summarizing tables/functions/routes.
- Print a final summary: files touched, any freeze deviations + why.
