# Task S1 — Server: issues mirror worker, projections, gateway, WS RPCs

Work in the current repo root (a pathwayOS worktree). Effect 4, SQLite, Bun.

READ FIRST:

1. docs/issues-build/interface-freeze.md §0.2, §5, §3, §10 — BINDING.
2. /private/tmp/claude-501/-Users-coreybaines-GitHub-pathwayapp/21f4374e-b3a6-4301-813d-f1f7860f47f0/scratchpad/scout-server.md — persistence/RPC/worker patterns (§1, §2, §6, §7 + Key risks).
3. /private/tmp/claude-501/-Users-coreybaines-GitHub-pathwayapp/21f4374e-b3a6-4301-813d-f1f7860f47f0/scratchpad/scout-convex.md §2–4 — credential gating + HTTP envelope (CloudSyncWorker/EmailSandboxSyncWorker are your worker exemplars).
4. packages/contracts/src/issues.ts + apps/server/src/issues/IssuesGateway.ts + apps/server/src/issues/delegation/IssueDelegationService.ts (seam tags, already authored — implement against them, do not modify them).

DELIVERABLES (ownership — ONLY these):

- apps/server/src/issues/IssuesCommandClient.ts (new) — Effect service wrapping HttpClient POSTs to `${httpActionsUrl}/v1/issues/command|mirror|detail` with the environment-credential bearer (same gating/readCredential/readHttpActionsUrl approach as CloudSyncWorker; if credential or URL missing → typed "offline" failure).
- apps/server/src/persistence/Migrations/037*IssuesMirror.ts (new) + register in Migrations.ts (append import + entry) — tables per freeze §5: issues_mirror*<collection> for all 12 collections (snake_case, JSON TEXT for nested structs like cycleConfig/repoLinks/filters/display/labelIds) + issues_mirror_state(scope TEXT PRIMARY KEY, cursor_seq INTEGER, synced_at TEXT, last_error TEXT, workspace_key TEXT, viewer_user_id TEXT). Follow 005_Projections.ts conventions (IF NOT EXISTS, indexes on natural keys).
- apps/server/src/issues/IssuesMirrorStore.ts (new) — repository service over those tables: applyDeltaBatch(rows, nextSeq) in ONE transaction (upserts per table; purged issues delete the row), getSnapshot() → IssuesSnapshot (contracts type; online/syncedAt from state row), cursor get/set, plus an internal PubSub<IssuesStreamItem>: publish upsert/remove items AFTER the transaction commits (see scout-server "Key risks" #1) and a changes Stream. SqlSchema statement style per Layers/ProjectionThreads.ts.
- apps/server/src/issues/IssuesMirrorWorker.ts (new) — Context.Service {syncOnce, start} exactly like EmailSandboxSyncWorker: poll loop in forkScoped; syncOnce pulls mirrorDelta(sinceSeq=cursor, limit 500) via IssuesCommandClient, applies via store, loops while hasMore; publish {kind:"status", online, syncedAt} transitions; sleep 2s between polls (keep it simple: fixed 2s; the freeze's 2s/10s adaptive interval is optional — if trivial, gate on whether any issues subscription is active via a subscriber-count Ref exposed by the store/gateway; otherwise fixed 2s and note it).
- apps/server/src/issues/IssuesGatewayLive.ts (new) — Layer providing IssuesGateway: execute = guardrail pre-check (agent attribution: forbid issue.purge; force nothing else locally — Convex is final authority) then IssuesCommandClient POST /command (map offline/HTTP errors to IssuesDomainError codes) then trigger an immediate syncOnce (fire-and-forget) so the UI converges fast; getSnapshot/changes from the store; getIssueDetail = client POST /detail; detailChanges(issueId) = emit initial detail then re-fetch+emit whenever store.changes has an upsert for that issueId (debounce 300ms), plus 10s periodic refresh while subscribed.
- ws.ts — add 4 handlers (issues.subscribe: Stream.concat(snapshot frame, store changes tail); issues.subscribeDetail; issues.execute with attribution {kind:"human"}; issues.delegationState via IssueDelegationService.state) + RPC_REQUIRED_SCOPE entries (read scope for subscribes/delegationState, write scope for execute — mirror how orchestration read/write scopes are assigned).
- server.ts + serverRuntimeStartup.ts — wire layers (IssuesCommandClient, IssuesMirrorStore, IssuesMirrorWorker, IssuesGatewayLive) and start the worker in the reactors.start phase like cloudSyncWorker. If IssueDelegationService has no Live layer in this worktree yet (it is built in a parallel wave), wire a STUB layer `IssueDelegationServiceUnavailableLive` (state returns empty DelegationQueueState with capacity from settings; start = no-op) in a separate file apps/server/src/issues/delegation/IssueDelegationServiceStub.ts and add a `// WIRING:` comment where the real layer swaps in; also write docs/issues-build/WIRING-s1.md noting it.

RULES:

- Never use `any`. Match Layers/Services/Errors conventions exactly (toPersistenceSqlError etc.).
- Do NOT run vp check / typecheck / lint / tests / dev servers. Validation deferred.
- Commit when done: `[new feature] Add issues mirror worker, gateway, and WS RPCs`, body summarizing.
- Print final summary: files, deviations + why.
