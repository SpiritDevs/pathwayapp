# Task S2 — MCP issues toolkit

Work in the current repo root (a pathwayOS worktree). Effect 4.

READ FIRST:
1. docs/issues-build/interface-freeze.md §6 (+ §2 for types, §10: do NOT expose issue.setDelegationStatus as a tool) — BINDING.
2. /private/tmp/claude-501/-Users-coreybaines-GitHub-pathwayapp/21f4374e-b3a6-4301-813d-f1f7860f47f0/scratchpad/scout-server.md §4 — MCP anatomy + "to register a new toolkit" checklist.
3. Exemplars: apps/server/src/mcp/toolkits/email/{tools.ts,handlers.ts}, McpInvocationContext.ts, McpSessionRegistry.ts, McpHttpServer.ts, emailSandbox/AgentEmailSandbox.ts (project resolution pattern).
4. packages/contracts/src/issues.ts + apps/server/src/issues/IssuesGateway.ts (seam — implement against, do not modify).

DELIVERABLES (ownership — ONLY apps/server/src/mcp/**):
- McpInvocationContext.ts — extend McpCapability with "issues" + requireIssuesCapability(operation) guard + a tagged IssueAgentAccessError (mirror EmailAgentAccessError).
- McpSessionRegistry.ts — mint capabilities ["preview","email","issues"].
- toolkits/issues/tools.ts — Tool.make definitions for: issue_create, issue_get, issue_list, issue_update, issue_comment, issue_start_work, issue_link_thread, issue_relation_set, issue_delete, team_list, actor_list, state_list, label_list, cycle_list, epic_list, view_list. Parameters/success schemas built from @pathwayos/contracts issues types (reuse the entity schemas; success for lists = arrays of entities; issue_get = { issue, detail }). Failure type IssueAgentToolError (tagged, mirror EmailAgentToolError). Read tools annotated Tool.Readonly like email; write tools without readonly annotations but marked non-destructive where true (soft-delete) per email file idiom. Descriptions: crisp, agent-facing, mention guardrails (created issues land in Triage; deletes are soft; purge unavailable).
- toolkits/issues/handlers.ts — IssuesToolkit.toLayer({...}): each handler requireIssuesCapability → resolve agent actor via AgentActorResolver → IssuesGateway call with attribution {kind:"agent", actorId, threadId: scope.threadId}. issue_list: filter the gateway snapshot in-handler (teamId/teamKey, state category, stateId, assigneeActorId, labelId, priority, text substring over title+identifier, includeCompleted flag default false, limit default 50). issue_get: accepts issueId OR identifier (resolve via snapshot, aliases not required). issue_start_work: gateway.execute({type:"issue.startWork", ...}). issue_update: restrict patch keys to the freeze issue.update set (no delegationStatus).
- toolkits/issues/AgentActorResolver.ts — service resolving scope.threadId → IssueActorId: (1) snapshot threadLinks: link for this threadId whose issue has an agent assignee → that actor; (2) else find an agent actor in the snapshot whose id is recorded in ServerSettings.agentActors and matches the thread's providerInstanceId (via ProjectionSnapshotQuery thread shell → modelSelection.instanceId); (3) else create a default agent actor via gateway.execute({type:"agent.create", displayName: provider instance display name or "Agent", avatarColor deterministic from instanceId, config:{providerInstanceId, model:null, instructions:null}}) and return createdId. Memoize per threadId.
- McpHttpServer.ts — IssuesToolkitRegistrationLive = McpServer.toolkit(IssuesToolkit).pipe(Layer.provide(IssuesToolkitHandlersLive), Layer.provide(AgentActorResolver.layer)); merge into PathwayToolkitRegistrationLive; provide IssuesGateway from the ambient layer graph (add to the layer's dependencies the same way AgentEmailSandbox is provided — check how email gets its services and mirror it; if the gateway must be passed through server.ts wiring, note it in docs/issues-build/WIRING-s2.md instead of editing server.ts, which you must NOT touch).

RULES:
- Never use `any`. Match email toolkit style closely (error taxonomy, annotations, naming).
- Do NOT run vp check / typecheck / lint / tests / dev servers. Validation deferred.
- Commit when done: `[new feature] Add write-capable MCP issues toolkit for agents`, body summarizing tools + guardrails.
- Print final summary: files, deviations + why, and any WIRING notes.
