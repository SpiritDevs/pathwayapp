# Task S3 — Delegation: capacity queue, headroom monitor, auto-spawn

Work in the current repo root (a pathwayOS worktree). Effect 4.

READ FIRST:

1. docs/issues-build/interface-freeze.md §5 (delegation bullets), §9, §10 — BINDING.
2. /private/tmp/claude-501/-Users-coreybaines-GitHub-pathwayapp/21f4374e-b3a6-4301-813d-f1f7860f47f0/scratchpad/scout-server.md §5 (thread spawning — the exact bootstrap command shape), §3 (dispatch), §6 (settings), §7 (queue/reactor precedents).
3. Seams (implement against, do NOT modify): apps/server/src/issues/IssuesGateway.ts, apps/server/src/issues/delegation/IssueDelegationService.ts, packages/contracts/src/issues.ts, contracts settings (issueDelegation, agentActors).
4. Exemplars: orchestration/Layers/OrchestrationEngine.ts (queue+worker), Layers/ProviderCommandReactor.ts (reactor), diagnostics/ProcessResourceMonitor.ts, emailSandbox project binding (logical project key resolution).

DELIVERABLES (ownership — ONLY apps/server/src/issues/delegation/\*\* and docs/issues-build/WIRING-s3.md):

- delegation/SystemHeadroom.ts (new) — service sampling os.cpus()/os.freemem() (two-sample CPU delta over ~500ms), returning { cpuPercent: number|null, freeMemoryMb: number|null }; failures → nulls (headroom passes on null per freeze §9).
- delegation/IssueDelegationServiceLive.ts (new) — Layer providing IssueDelegationService:
  - State: in-memory queue Ref (entries {issueId, actorId, priority, enqueuedAt}) + running map Ref ({issueId → {actorId, threadId, startedAt}}).
  - Startup: rebuild queue from IssuesGateway.getSnapshot() — issues with delegationStatus "queued"|"starting" assigned to agent actors owned by this instance (owned = actor id present in ServerSettings.agentActors); "running" issues whose linked thread still exists → adopt into running map; stale "running"/"starting" with no live thread → issue.setDelegationStatus completed? NO — set back to "queued" and re-enqueue.
  - Reactor: subscribe IssuesGateway.changes; on issues upsert: (a) assigned to owned agent actor && delegationStatus null && state category in {triage, backlog, unstarted} → gateway.execute issue.setDelegationStatus "queued" (attribution agent) + enqueue (dedupe by issueId); (b) assignee changed away or state category canceled/completed while queued → dequeue + setDelegationStatus null; (c) thread lifecycle: subscribe OrchestrationEngine.streamDomainEvents for turn-completion/thread-deletion events of running threads → on turn end mark completed (setDelegationStatus "completed", threadLink.update status "closed" optional: keep "linked"), remove from running, pump the queue.
  - Pump loop (single worker fiber, forkScoped): while queue non-empty && running.size < settings.issueDelegation.maxConcurrent && settings.issueDelegation.enabled: check SystemHeadroom (cpuPercent < cpuHeadroomPercent && freeMemoryMb > minFreeMemoryMb; null passes); if headroom fails sleep 15s and retry; else dequeue best entry (priority asc where 0 sorts LAST i.e. order [1,2,3,4,0], then FIFO) and spawn.
  - Spawn: resolve repo — issue's team defaultRepoLogicalKey (or single repoLink) → match against local projects via ProjectionSnapshotQuery shell snapshot projects' repositoryIdentity canonical keys (mirror how email sandbox binds logical project keys; fallback: match workspaceRoot basename). No repo resolvable → setDelegationStatus "failed" + gateway.execute comment.create explaining why (attribution agent). Else: setDelegationStatus "starting"; build prompt per freeze §9 (identifier, title, description via getIssueDetail, team name, priority label, labels, due date, parent + sub-issue identifiers, last 20 comments formatted `author: body`, standing instructions from settings.agentActors[actorId].instructions, closing instructions to post progress via issue_comment and set state via issue_update when done); dispatch ONE thread.turn.start with bootstrap.createThread {projectId, title: identifier + " " + title, modelSelection from agentActors[actorId] (providerInstanceId/model; fallback: project default via snapshot or settings textGenerationModelSelection — inspect what exists), runtimeMode from config or "full-access", interactionMode "default", branch/worktree: null (local mode)} via OrchestrationEngine.dispatch with serverCommandId-style commandId (mirror ws.ts dispatchBootstrapTurnStart construction; reuse its helpers if exported, else replicate minimal logic — do NOT edit ws.ts); then gateway.execute threadLink.create {issueId, threadId, environmentId} + issue.update {stateId: first started-category state for the issue's scope} + setDelegationStatus "running" (all attribution agent).
  - state: Effect returning DelegationQueueState (running/queued arrays + capacity {maxConcurrent, cpuPercent, freeMemoryMb, headroomOk}).
- docs/issues-build/WIRING-s3.md — exact layer wiring lines the integration pass must add to server.ts/serverRuntimeStartup.ts (IssueDelegationServiceLive replacing any stub, SystemHeadroom layer, start() in reactors phase after the mirror worker).

RULES:

- Never use `any`. Effect idioms per exemplars (Ref, Queue only if it fits, forkScoped fibers, tagged errors).
- Do NOT touch ws.ts / server.ts / serverRuntimeStartup.ts / mcp/\*\* / issues root files other than delegation/.
- Do NOT run vp check / typecheck / lint / tests / dev servers. Validation deferred.
- Commit when done: `[new feature] Add issue delegation queue with capacity and headroom gating`, body summarizing.
- Print final summary: files, deviations + why.
