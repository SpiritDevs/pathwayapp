# Task F3 — Client runtime state + web atoms for issues

Work in the current repo root (a pathwayOS worktree). Bun monorepo, React 19, Effect atoms.

READ FIRST:

1. docs/issues-build/interface-freeze.md §2, §3, §7 — BINDING.
2. /private/tmp/claude-501/-Users-coreybaines-GitHub-pathwayapp/21f4374e-b3a6-4301-813d-f1f7860f47f0/scratchpad/scout-web.md — §1 is your playbook (factory pattern, referential stability, command atoms).
3. packages/contracts/src/issues.ts — the types.
4. Exemplars: packages/client-runtime/src/state/{shell.ts, shellReducer.ts, threadShell.ts, projectEntities.ts, threads.ts, threadDetail.ts, entities.ts, models.ts, runtime.ts}; apps/web/src/state/{shell.ts, threads.ts, projects.ts, entities.ts, query.ts, use-atom-command.ts}.

DELIVERABLES (exclusive ownership — touch ONLY these):

- packages/client-runtime/src/state/issuesReducer.ts (new) — pure reducer applying IssuesStreamItem to an IssuesSnapshot (upsert by table tag, remove on purge, status merges online/syncedAt, seq-gated like applyShellStreamEvent; unknown kinds/tables ignored for forward compat).
- packages/client-runtime/src/state/issuesState.ts (new) — `makeEnvironmentIssuesState` subscribing WS_METHODS.issuesSubscribe (mirror shell.ts makeEnvironmentShellState incl. cached-snapshot persistence if the shell does it — follow the same cache util), and `createEnvironmentIssuesAtoms({ catalogValueAtom, snapshotAtom })` following threadShell.ts EXACTLY in structure: per-environment collection atoms, index maps, refs arrays with referential-stability guards (copy the \*RefsEqual pattern), per-entity atom families, and grouped-refs families for groupBy: state, assignee, priority, label (first label), cycle, epic, team. Add `issueKey/parseIssueKey/issueRefsEqual` additions to entities.ts and a `scopeIssue` model to models.ts (small append edits are allowed in those two files).
- packages/client-runtime/src/state/issueDetail.ts (new) — `createEnvironmentIssueDetailAtoms` subscribing WS_METHODS.issuesSubscribeDetail per issue ref (mirror threads.ts + threadDetail.ts: state atom family with idle TTL, slices for detail/comments/reactions/events; comments assembled into a thread tree helper `buildCommentTree(comments)` exported pure).
- packages/client-runtime/src/state/issuesCommands.ts (new) — `createIssuesEnvironmentAtoms(runtime)` exposing `execute` (createEnvironmentRpcCommand on WS_METHODS.issuesExecute) plus typed convenience wrappers (createIssue, updateIssue, moveTeam, deleteIssue, restoreIssue, purgeIssue, startWork, createComment, updateComment, deleteComment, toggleReaction, createRelation, deleteRelation, linkThread, unlinkThread, createTeam, updateTeam, deleteTeam, addTeamMember, removeTeamMember, createState, updateState, deleteState, createLabel, updateLabel, deleteLabel, updateCycle, createEpic, updateEpic, deleteEpic, createMilestone, updateMilestone, deleteMilestone, createView, updateView, deleteView, createAgent, updateAgent, deleteAgent, updateWorkspace) — each a thin wrapper building the IssueCommand and invoking execute; plus `delegationState` via createEnvironmentRpcQueryAtomFamily on WS_METHODS.issuesDelegationState (staleTimeMs 2000, refreshIntervalMs 5000).
- packages/client-runtime exports index — export the new modules like siblings.
- apps/web/src/state/issues.ts (new) — instantiate all factories against connectionAtomRuntime/environmentCatalog exactly as state/threads.ts does; export `environmentIssues`, `issueDetails`, `issuesEnvironment`.
- apps/web/src/state/issueEntities.ts (new) — React hooks per freeze §7 (useIssues, useIssue, useIssueDetail, useIssueTeams, useIssueStates, useIssueLabels, useIssueActors, useIssueCycles, useIssueEpics, useIssueMilestones, useIssueSavedViews, useIssueRelations, useIssueThreadLinks, useDelegationState, useIssuesSnapshotMeta [online/syncedAt/workspaceKey/viewerUserId], grouped-refs hooks) + non-hook readIssue/findIssueRef via appAtomRegistry. Null-ref frozen EMPTY guards like state/entities.ts.

RULES:

- Referential stability guards are load-bearing (virtualized lists) — replicate them everywhere.
- Never use `any`. Match neighboring code style precisely.
- Do NOT run vp check / typecheck / lint / tests / dev servers. Validation deferred.
- Commit when done: `[new feature] Add issues client-runtime state and web atoms`, body summarizing modules.
- Print final summary: files, deviations + why.
